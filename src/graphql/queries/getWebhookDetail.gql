query getWebhookDetail($client: String!){
    getWebhookDetail(client: $client){
        _id
        client
        url
        isLastTimeSuccess
        contentType
        secret
        enable
    }
}